{
  "name": "@harness/desktop",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "HarnessAgentOS Electron desktop app",
  "main": "out/main/main.js",
  "scripts": {
    "ensure:electron": "node -e \"require('electron/install.js')\"",
    "rebuild:electron": "npm run ensure:electron && electron-rebuild -f -w better-sqlite3",
    "rebuild:node": "npm rebuild better-sqlite3 --update-binary --ignore-scripts=false",
    "dev": "npm run rebuild:electron && electron-vite build && electron .",
    "dev:watch": "electron-vite build --watch",
    "build": "electron-vite build",
    "check": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
    "smoke:e2e": "npm run rebuild:node --silent && node --import tsx scripts/smoke-e2e-runner.mjs",
    "smoke:agent-fake": "npm run rebuild:node --silent && node --import tsx scripts/smoke-agent-fake.mjs",
    "smoke:agent-live": "npm run rebuild:node --silent && node --import tsx scripts/smoke-agent-live.mjs",
    "smoke:a2a-remote-live": "npm run rebuild:node --silent && node --import tsx scripts/smoke-a2a-remote-live.mjs",
    "smoke:all": "npm run smoke:e2e && npm run smoke:agent-fake && npm run e2e && npm run rebuild:node",
    "e2e": "npm run rebuild:electron && npm run build && playwright test"
  },
  "dependencies": {
    "@harness/agent": "*",
    "@harness/core": "*",
    "@harness/evals": "*",
    "@harness/learner": "*",
    "@harness/orchestration": "*",
    "@harness/quality": "*",
    "@harness/runners": "*",
    "@harness/skillify-adapter": "*",
    "@harness/storage": "*",
    "better-sqlite3": "^12.10.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "@playwright/test": "^1.59.1",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.7.4",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "41.6.1",
    "electron-vite": "^5.0.0",
    "playwright": "^1.59.1",
    "typescript": "^5.6.3",
    "vite": "^7.3.3"
  }
}
